# 📘 KnowledgeBaseAI 2.0 — Master Design Document

**Версия:** 2.1-RELEASE (Post-Audit)
**Статус:** Утверждено к производству
**Дата:** 16 декабря 2025
**Тип документа:** Single Source of Truth

---

## 📑 Оглавление

1.  **Обзор системы (System Overview)**
2.  **Архитектурный Манифест (Invariants)**
3.  **Технологический стек и Инфраструктура**
4.  **Модель данных и Онтология**
5.  **Механизм мутаций: Ingestion & Proposals**
6.  **Математическое ядро (Math Core)**
7.  **Безопасность и Мультитенантность**
8.  **Frontend & UX паттерны**
9.  **Observability, SLO & DevOps**
10. **Advanced: Migrations & Versioning**

---

## 1. Обзор системы (System Overview)

**KnowledgeBaseAI** — это Enterprise-платформа адаптивного обучения, использующая **Graph-RAG** подход. Система строит направленный граф зависимостей навыков для Gap Analysis и генерации нелинейных образовательных маршрутов.

---

## 2. Архитектурный Манифест (System Invariants)

Нарушение этих правил приравнивается к дефекту архитектуры.

1.  **Graph-First Source of Truth**
    *   **Neo4j** — единственный источник истины.
    *   **Vector DB (Qdrant)** — производный кэш.
    *   **Sync Job:** Существует фоновый процесс (Cron/Event), который обеспечивает eventual consistency. При рассинхроне выполняется принудительный Reindex из графа.

2.  **Safety via Proposals (Event-Sourced Mutation)**
    *   Прямая запись (`CREATE`, `SET`) запрещена.
    *   Цепочка: `Draft → Proposal → Validation → Rebase → Integrity Gate → Commit`.
    *   **Принцип Rebase:** Разрешение конфликтов происходит **только по ID**. Никаких попыток семантического слияния ("угадывания смысла") на лету.

3.  **Strict Tenant Isolation (P0)**
    *   Изоляция данных на уровне DAO и Vector Payload.
    *   Утечка данных — инцидент P0.

4.  **Determinism & Idempotency**
    *   Одинаковый файл + одинаковый конфиг = **Идентичный Hash Proposal**.
    *   Это требует строгой нормализации входных данных (см. раздел 5.1).

---

## 3. Технологический стек

*   **Backend:** Python 3.12, FastAPI.
*   **Data:** Neo4j 5.x (Graph), Qdrant (Vector), PostgreSQL 16 (Meta/Auth), Redis (Queue/Cache).
*   **Storage:** S3-compatible (MinIO/AWS).
*   **Infra:** Docker Compose / K8s, Traefik.

---

## 4. Модель данных и Онтология

### 4.1 Жизненный цикл узлов (Node Lifecycle)
Каждый узел обязан иметь поле `lifecycle_status`:
*   `ACTIVE`: Используется в поиске и Roadmaps.
*   `DEPRECATED`: Исключен из генерации *новых* Roadmaps, но остается для исторических отчетов и совместимости.
*   `ARCHIVED`: Скрыт, доступен только администраторам, кандидат на удаление.

### 4.2 Типы узлов (Nodes)
Обязательные поля: `uid`, `tenant_id`, `lifecycle_status`, `created_at`.

| Тип | Описание |
| :--- | :--- |
| **Concept** | Теоретическая единица знания. |
| **Skill** | Практический навык. |
| **Method** | Способ решения/выполнения. |
| **Error** | Типичная ошибка (Anti-pattern). |
| **Assessment** | Единица контроля (тест/задача). |
| **SourceChunk**| Единица Evidence (цитата). |

### 4.3 Типы связей (Relationships)
| Связь | Правило Integrity |
| :--- | :--- |
| **PREREQ** | DAG (Ацикличность обязательна). |
| **BASED_ON** | Skill → Concept (Обязательно min 1). |
| **PART_OF** | Иерархия (Strict Tree внутри подграфа). |
| **EVIDENCED_BY**| Любой узел → SourceChunk (Обязательно для AI-мутаций). |

---

## 5. Механизм мутаций: Ingestion & Proposals

### 5.1 Canonicalization (Критично для Идемпотентности)
Перед генерацией `doc_fingerprint` и `proposal_checksum` текст проходит жесткую нормализацию:
1.  **Unicode:** NFKC normalization.
2.  **Whitespace:** `text.strip().replace(/\s+/, ' ')` (схлопывание пробелов).
3.  **Encoding:** UTF-8.
4.  **Structure:** JSON ключи сортируются по алфавиту перед хешированием.

### 5.2 Ingestion Pipeline
1.  **Upload & Norm:** Загрузка + Нормализация (см. 5.1).
2.  **Chunk & Embed:** Генерация векторов.
3.  **Align:** Поиск кандидатов в графе (по векторам и именам).
4.  **Proposal:** Генерация JSON-черновика.

### 5.3 Integrity Gate (Performance Optimized)
Проверка целостности выполняется **только на затронутом подграфе**:
1.  Строится локальный граф изменений + N шагов вглубь (где N=2..3).
2.  Проверяются циклы `PREREQ` только в этом контексте.
3.  Проверяются "висячие" ссылки.
*Если проверка занимает > 500ms, Proposal помечается как `ASYNC_CHECK_REQUIRED`.*

---

## 6. Математическое ядро (Math Core)

### 6.1 Cost Function
$$ W_{edge} = \text{Clip}\left( (W_{static} \cdot G_{diff}) \cdot (1 + Decay \cdot (1 - U_{conf})), 0.1, 10 \right) $$

### 6.2 Roadmap Stability (Jitter Control)
Для предотвращения резких скачков маршрута при малых изменениях весов:
*   **Hysteresis:** Маршрут перестраивается, только если новый путь "дешевле" старого минимум на 15%.
*   **G_diff Smoothing:** Глобальная сложность обновляется через скользящее среднее (EMA, window=7 days).

---

## 7. Безопасность и Мультитенантность

### 7.1 Isolation Layers
1.  **API:** JWT Validation → Extraction `tenant_id`.
2.  **DAO:** Принудительный `WHERE tenant_id = $ctx_id` во всех запросах.
3.  **Vector:** Payload Filter `{ must: [{ key: "tenant_id", match: { value: $id } }] }`.

### 7.2 Scaling Strategy
*   **Tier 1 (Small/Medium):** Shared DB + Logical Isolation (Row Level Security pattern).
*   **Tier 2 (Enterprise):** Database-per-Tenant (Neo4j Fabric или отдельные инстансы/базы) для физической изоляции и SLA.

---

## 8. Frontend & UX

### 8.1 Transactional Optimistic UI
*   Frontend ведет **Transaction Log** (очередь мутаций).
*   При действии пользователя:
    1.  Генерируется `tx_id`.
    2.  Применяется локальный патч.
*   При ошибке (Server Error / Integrity Fail):
    1.  Находится транзакция по `tx_id`.
    2.  Выполняется **Inverse Patch** (откат только этой транзакции).
    3.  Пользователь получает уведомление, данные не перезагружаются полностью (чтобы не сбросить скролл/фокус).

### 8.2 HITL Review Interface
Для `Risk=HIGH`:
*   Side-by-side diff.
*   Визуализация затрагиваемого подграфа (Impact Analysis).

---

## 9. Observability, SLO & DevOps

### 9.1 Key Metrics (Prometheus)
*   **Ingestion Success Rate:** `%` успешных тасок.
*   **Integrity Violation Total:** Цель = 0.
*   **Proposal Auto-Rebase Rate:** `%` автоматически принятых изменений.
*   **Roadmap Generation Latency (p95):** Цель < 200ms (cached), < 1s (fresh).

### 9.2 Tracing
Сквозной `trace_id` от Nginx до Disk I/O Neo4j.

---

## 10. Advanced: Migrations & Versioning

### 10.1 Schema Migration Strategy
Т.к. Neo4j schema-less, миграции контролируются на уровне приложения:
1.  **Schema Version:** Хранится в Postgres (`tenant_settings`).
2.  **Migration Script:** Python-скрипт, выполняющий Cypher-запросы (например, `MATCH (n:OldLabel) SET n:NewLabel REMOVE n:OldLabel`).
3.  **Gatekeeper:** Приложение не стартует, если `Code.SchemaVersion > DB.SchemaVersion`.

### 10.2 Audit & Time Travel
*   Все примененные Proposals сохраняются в `audit_log` (Postgres/S3).
*   **Time Travel (ограниченный):** Возможность "откатить" граф до состояния на дату X реализуется через применение обратных операций (`revert_ops`) из лога, начиная с текущего момента назад.
